import { Flex } from "antd";
import React from "react";

const Snapshot = ()=>{
    return(
        <Flex gap={30} align='center' vertical>
            <div style={{padding:100, background:'aliceblue'}}>Hello Snapshot...</div>
        </Flex>
    )
}

export default Snapshot;